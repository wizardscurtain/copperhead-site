{
  "name": "copperhead-consulting-website",
  "version": "1.0.0",
  "description": "Professional security consulting website with PWA capabilities",
  "private": true,
  "engines": {
    "node": ">=18.0.0 <=20.19.5",
    "yarn": ">=1.22.0"
  },
  "scripts": {
    "build": "cd frontend && yarn install --frozen-lockfile && yarn build",
    "dev": "cd frontend && yarn dev",
    "lint": "cd frontend && yarn lint", 
    "start": "cd frontend && yarn start"
  }
}
