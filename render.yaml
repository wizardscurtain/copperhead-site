# Render Blueprint for Copperhead Consulting Inc
# PostgreSQL + Static Hosting Configuration

services:
  # Backend API Service (Python)
  - type: web
    name: copperhead-backend
    runtime: python
    plan: starter
    autoDeploy: true
    buildCommand: |
      python -m pip install --upgrade pip
      pip install --no-cache-dir -r requirements.txt
    startCommand: |
      python -m alembic upgrade head
      python server.py
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.5
      - key: DATABASE_URL
        fromDatabase:
          name: copperhead-db
          property: connectionString
      - key: ENVIRONMENT
        value: production
      - key: CSRF_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true
      - key: RESEND_API_KEY
        value: ""  # Set manually in dashboard or via Render UI secret

  # Frontend Static Site (React/Vite build output)
  - type: web
    name: copperhead-frontend
    runtime: static
    rootDir: frontend
    buildCommand: |
      yarn install --frozen-lockfile || yarn install
      yarn build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    autoDeploy: true
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
      - path: /videos/*
        name: Cache-Control
        value: public, max-age=31536000
      - path: /images/*
        name: Cache-Control
        value: public, max-age=31536000
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000
    # SPA fallback (all unmatched paths serve index.html)
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  # PostgreSQL Database
  - name: copperhead-db
    # Plan omitted: Render will provision using current default tier. Set manually after creation if needed.
    postgresMajorVersion: "15"
